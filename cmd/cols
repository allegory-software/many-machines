#!/bin/bash
# MYSQL | [DB=] TABLE | list MySQL columns
. mmlib

TABLE="$1"
checkvars TABLE

list_cols() {
	local DB=${DB:-$DEPLOY}
	checkvars DB
	ssh_script "MYSQL_PRETTY=1 query_on $DB 'show columns from $TABLE'"
}
each_md list_cols $TABLE
