- install:
	- gen mon.conf from mon_* vars
	- if installing locally, make a symlink to mon dir instead.

sampling:
	- report used ram not free
	- report used disk space not free
	- min disk is 10%
	- THINK: what if RAM/DISK size change? maybe we need to log daily min & max ?

tui:
	- show out-of-range intervals with time, duration, min, max, avg
		- THINK: what about when it trickles in/out of range?

alerting sources:
	- TODO: monitor OOM events
	- TODO: monitor DRBD events
	- TODO: monitor SSL cert. expiration
	- TODO: monitor backups age and validate last backup (checksum check, dry-restore)
	- TODO: monitor deployment age

app autorestart:
	- RAM limit for app service (MemoryMax=; let OOM report kills which signal leaks)
	- TODO: probe ping URL using curl and restart the app if not responding.
