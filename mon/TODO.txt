tui:
	- show out-of-range intervals with time, duration, min, max, avg
		- THINK: what about when it trickles in/out of range?
	- show intervals in which mon was down (use RAM to compute).

alerting sources:
	- TODO: monitor DRBD events
	- TODO: monitor SSL cert. expiration
	- TODO: monitor backups age and validate last backup (checksum check, dry-restore)
	- TODO: monitor deployment age
	- tracking ram usage of my app process is more valuable than total ram.
	let's add a ram proble for a single process but don't lock on a pid.
	make a probe_ram_for function in mon that takes a string to match against cmdline.

app autorestart:
	- RAM limit for app service MemoryMax=, then alert on crash count.
	- TODO: probe ping URL using curl and restart the app if not responding.
